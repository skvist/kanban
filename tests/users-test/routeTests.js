'use strict';

var chai = require('chai');
var expect = chai.expect;

describe('User test', function () {
    it('expect true', function (done) {
        expect(true).to.equal(true);
        done();
    });
});
